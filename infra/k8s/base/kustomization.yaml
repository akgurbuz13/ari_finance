apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ari

resources:
  - namespace.yaml
  - core-banking.yaml
  - blockchain-service.yaml
  - web.yaml
  - admin-console.yaml
  - ingress.yaml
  # Security hardening
  - network-policies.yaml
  - pod-disruption-budgets.yaml
  # External secrets (requires external-secrets-operator installed in cluster)
  - external-secrets.yaml

commonLabels:
  app.kubernetes.io/part-of: ari-platform
  app.kubernetes.io/managed-by: kustomize

# Security-related patches applied to all deployments
patches:
  # Add security labels
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /metadata/labels/security.ari.io~1compliance
        value: "required"
