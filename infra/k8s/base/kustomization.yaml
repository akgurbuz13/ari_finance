apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ova

resources:
  - namespace.yaml
  - core-banking.yaml
  - blockchain-service.yaml
  - web.yaml
  - admin-console.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: ova-platform
  app.kubernetes.io/managed-by: kustomize
