apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-banking
  namespace: ova
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: core-banking
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 2Gi
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ova-ingress
  namespace: ova
spec:
  tls:
    - hosts:
        - api.ova.app
        - app.ova.app
        - admin.ova.app
      secretName: ova-eu-tls
  rules:
    - host: api.ova.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: core-banking
                port:
                  number: 8080
    - host: app.ova.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 3000
    - host: admin.ova.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-console
                port:
                  number: 3001
